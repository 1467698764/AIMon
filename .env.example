PORT=8787
# 可信反向代理跳数：直连为 0，单层反代为 1；不要大于实际可信代理层数
AIMON_TRUST_PROXY=1
DATA_DIR=./data
# Docker 镜像默认强制校验 DATA_DIR 是否位于独立挂载卷；本地 Node 开发无需设置
# REQUIRE_PERSISTENT_DATA=false
AIMON_SECRET=replace-with-at-least-32-random-characters
# 推荐公网首次部署时设置；数据库已有管理密码后不会覆盖
AIMON_BOOTSTRAP_PASSWORD=
REQUEST_TIMEOUT_MS=30000
# Production blocks loopback, private-network and cloud-metadata destinations by default.
# Enable only when AIMon intentionally monitors a trusted service on the same private network.
AIMON_ALLOW_PRIVATE_NETWORK=false
# 可选的第二层 HTTP Basic Auth；留空时只使用页面内管理密码
AIMON_BASIC_USER=
AIMON_BASIC_PASSWORD=
CLOAKBROWSER_ENABLED=true
CLOAKBROWSER_HEADLESS=true
CLOAKBROWSER_TIMEOUT_MS=60000
CLOAKBROWSER_IDLE_MS=180000
CLOAKBROWSER_MAX_CONTEXTS=2
# Optional HTTP/SOCKS5 proxy. Treat this value as a secret when it contains credentials.
CLOAKBROWSER_PROXY=
# Optional local Chrome/Chromium path used when the dedicated binary cannot be downloaded.
CLOAKBROWSER_BINARY_PATH=
CLOAKBROWSER_AUTO_UPDATE=false
# Optional CloakBrowser Pro license.
CLOAKBROWSER_LICENSE_KEY=
